class Ball{
  constructor(x, y, w, h, vx, vy){
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
    this.vx = vx;
    this.vy = vy;
  }
}


function setup() {
  createCanvas(500, 500);
}

function draw() {
  background(225);
}